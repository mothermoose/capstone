<ul class="list-inline text-center">

  <a href="/projects"><button class="btn">All Projects</button></a>
  <br>
  <br>
</ul>
<h1 class="text-center"><%= @project.name %> Project</h1>
</br>

<!-- well -->
<div class="row">
    <div class="col-sm-12">
        <div class="well"><p><%= @project.description %></p> 
        </div>
    </div><!-- / col-sm-12 -->
</div><!-- / row -->
        <!-- / well -->  

<div class="row space-bottom-2x">
  <div class="col-sm-6">
      <h3>Teams</h3>
      <% @teams.each do |team| %>
        <div class="row">
            <div class="col-sm-4">
              <div class="panel panel-default panel-width">
                  <div class="panel-heading">
                     <h3 class="panel-title"><a href="/projects/<%=@project.id%>/team/<%=team.id%>"><%= team.name %></a></h3>
                  </div>
              <div class="panel-body">
            </div>
          </div><!-- / panel -->
        </div><!-- / panel -->             
      </div><!-- / col-sm-4 -->
      <% end %>
  </div>

  <div class="col-sm-6">
    

    <h3> Create a New Team </h3>
    <%= form_tag "/projects/#{@project.id}/teams", method: :post do %>
      <div class="form-group">
        <b>Team Name</b> <input type="text" class="form-control" name="name" value="<%= @team.name %>">
      </div>
      <div class="form-group">
        <b>Project Id</b> <input type="integer" class="form-control" name="project_id" value="<%= @team.project_id %>">
      </div>
      <input type="submit" class="btn btn-default" value="Create Team">
      <% end %>
  </div>
</div><!-- / row -->
        <!-- / 2col text blocks -->

<div class="row space-bottom-2x">
  <div class="col-sm-6">
  <h3>Tasks</h3>
    <% @tasks.each do |task| %>
      <div class="row">
        <div class="col-sm-4">
          <div class="panel panel-default panel-width">
            <div class="panel-heading">
              <h3 class="panel-title"><%= task.deadline %></a></h3>
            </div>
            <div class="panel-body">
              <p> <%= task.points %> pts </p>
              <p><%= task.description%></p>
            </div>
          </div><!-- / panel -->
        </div><!-- / panel -->             
      </div><!-- / col-sm-4 -->
    <% end %> 
  </div>
  <div class="col-sm-6">

    <h3> Create a New Task </h3>
    <%= form_tag "/tasks", method: :post do %>
      <div class="form-group">
        Deadline: <input type="date" class="form-control" name="deadline" value="<%= @task.deadline %>">
      </div>
      <div>
        Points: <input type="integer" class="form-control" name="points" value="<%= @task.points %>">
      </div>
      <div>
        Description: <input type="string" class="form-control" name="description" value="<%= @task.description %>">
      </div>
      <div>
        Project Id: <input type="integer" class="form-control" name="project_id" value="<%= @task.project_id %>">
      </div>
      <input type="submit" class="btn btn-default" value="Create New Task">
    <% end %>
  </div>
</div><!-- / row -->
        <!-- / 2col text blocks -->